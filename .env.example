# Example environment configuration for apigw-elk-full
# Copy this file to `.env` and set your values.
# PUBLIC_IP is the public IP (or domain) of your VPS where Keycloak, usersvc, and ELK are running.
# Do NOT include protocol (http/https); just the host/IP.
# Examples: 13.250.36.84 or api.example.com

PUBLIC_IP=<YOUR_VPS_PUBLIC_IP_OR_DOMAIN>
